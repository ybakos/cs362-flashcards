= link_to 'New Flashcard', new_flashcard_path, class: 'btn btn-primary float-right'
%h1 My Flashcards

- @flashcards.each do |flashcard|
  .flip-card.mt-5{style: 'margin: 0px 20px 20px 0px; display: inline-block;'}
    .flip-card-inner
      .flip-card-front
        %h2.card-title.my-5.py-5= flashcard.word
        %p.text-right
          = link_to 'edit', edit_flashcard_path(flashcard), style: 'margin-right: 20px;'
          = link_to '❌', flashcard, method: :delete, data: { confirm: 'Are you sure?' }
      .flip-card-back
        %p.my-5.py-5= flashcard.definition
